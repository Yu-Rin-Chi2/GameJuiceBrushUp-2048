/// <summary>
/// カード移動の方向を表す列挙型。
/// 入力イベントから生成され、ゲーム全体で使用される。
/// </summary>
public enum Direction
{
    /// <summary>上方向への移動</summary>
    Up,

    /// <summary>下方向への移動</summary>
    Down,

    /// <summary>左方向への移動</summary>
    Left,

    /// <summary>右方向への移動</summary>
    Right
}
